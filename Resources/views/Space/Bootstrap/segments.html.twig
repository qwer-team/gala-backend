{% extends 'GalaxyBackendBundle::layout.html.twig' %}

{% block title %}GalaxyBackendBundle:Space\Bootstrap:splitIntoSegments{% endblock %}

{% block body %}
    <h1>createSplitIntoSegment</h1>
    <form  class="form-search" action="{{ path('split') }}" method="post" {{ form_enctype(splitIntoSegmentForm) }}>        
        <div class="input-append">
            {{ form_widget(splitIntoSegmentForm.count) }}
            <button type="submit" class="btn">SplitIntoSegment</button>
        </div>
            {{ form_rest(splitIntoSegmentForm) }}
    </form>
{% endblock %}
