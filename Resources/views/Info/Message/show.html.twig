{% extends 'GalaxyBackendBundle::layout.html.twig' %}

{% block body %}
<div class="container" style="margin-top: 20px;">
    <h3>id {{message.id}}</h3>
    Картинка :{% if "img" in message|keys %}<img src="{{message.img}}"/><br>{% endif %}
    <form action="{{ path('update_message', {'id': message.id}) }}" method="post" {{ form_enctype(form) }}>
        {% if  message.moderatorAccepted is defined%}
            {{ form_row(form.moderatorAccepted, { 'attr': {'disabled': message.moderatorAccepted} }) }}
        {% endif %}
{{form_rest(form)}}
            <br>
            <button type="submit" class="btn btn-primary">Изменить</button>
<br />
</form>

<br />
<a href="{{ path('messages_list')}}">
    <button class="btn" type="button">Вернутся к списку</button>
</a>
</div>
{% endblock body %}